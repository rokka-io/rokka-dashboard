import styled from 'styled-components';

export const SidebarStacksList = styled.ul`
  overflow-x: hidden;
  overflow-y: auto;
  list-style-type: none;
  padding-left: 0;
  height: calc(100vh - 310px);
`;
